'use client';
import{useState,type FormEvent}from'react';import{Store}from'lucide-react';import{useAuth}from'@/lib/auth';
export default function Login(){const{login}=useAuth();const[tenantCode,setTenant]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);
 async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);try{await login({tenantCode:tenantCode.trim(),email:email.trim(),password})}catch(x){setError((x as Error).message)}finally{setBusy(false)}}
 return <main className="login"><form onSubmit={submit}><div className="brand"><Store/><span>Delivery Merchant</span></div><h1>Gestiona tu operación</h1><p>Pedidos, catálogo y sucursales en un solo lugar.</p><label>Código de empresa<input required autoComplete="organization" value={tenantCode}onChange={e=>setTenant(e.target.value)}/></label><label>Correo electrónico<input required type="email"autoComplete="email"value={email}onChange={e=>setEmail(e.target.value)}/></label><label>Contraseña<input required type="password"autoComplete="current-password"value={password}onChange={e=>setPassword(e.target.value)}/></label>{error&&<div role="alert"className="alert">{error}</div>}<button disabled={busy}>{busy?'Ingresando…':'Ingresar al portal'}</button></form></main>}
