'use client';import{FormEvent,useEffect,useState}from'react';import{api}from'@/lib/api';export default function Reset(){const[token,setToken]=useState<string|null>(null);useEffect(()=>setToken(new URLSearchParams(window.location.search).get('token')),[]);const[newPassword,setP]=useState('');const[done,setDone]=useState(false);async function submit(e:FormEvent){e.preventDefault();await api('/api/v1/auth/reset-password',{method:'POST',body:JSON.stringify({token,newPassword})});setDone(true)}return <main className="auth"><form onSubmit={submit}><h1>Nueva contraseña</h1>{done?<p>Contraseña actualizada. Ya puedes iniciar sesión.</p>:<><label>Contraseña<input type="password" value={newPassword} onChange={x=>setP(x.target.value)}/></label><button>Actualizar</button></>}</form></main>}
