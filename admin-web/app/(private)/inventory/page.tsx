'use client';import{useEffect,useState}from'react';import{api}from'@/lib/api';type S={productId:string;productName:string;quantity:number;lowStockThreshold:number|null;lowStock:boolean};export default function Inventory(){const[rows,setRows]=useState<S[]>([]);useEffect(()=>{api<S[]>('/api/v1/inventory').then(setRows)},[]);return <><div className="title"><div><p className="eyebrow">CATÁLOGO</p><h1>Inventario</h1><p>Stock general de productos con control de inventario.</p></div></div><section className="panel table">{rows.length===0?<div className="empty">No hay productos con inventario controlado.</div>:rows.map(x=><div className="tr" key={x.productId}><strong>{x.productName}</strong><span>{x.quantity}</span><span>{x.lowStockThreshold??'—'}</span><span className={`badge ${x.lowStock?'suspended':'active'}`}>{x.lowStock?'STOCK BAJO':'DISPONIBLE'}</span></div>)}</section></>}
