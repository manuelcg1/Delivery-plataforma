'use client';import{useEffect,useState}from'react';import{api}from'@/lib/api';type L={id:string;action:string;entityType:string;createdAt:string};export default function Audit(){const[rows,setRows]=useState<L[]>([]);useEffect(()=>{api<L[]>('/api/v1/audit-logs').then(setRows)},[]);return <><h1>Auditoría</h1><section className="panel table">{rows.map(x=><div className="tr" key={x.id}><span>{x.action}</span><span>{x.entityType??'—'}</span><span>{new Date(x.createdAt).toLocaleString()}</span></div>)}</section></>}
