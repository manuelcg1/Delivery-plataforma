'use client';import{useAuth}from'@/lib/auth';export default function Dashboard(){const{user}=useAuth();return <><header><div><p className="eyebrow">OPERACIÓN</p><h1>Hola, {user?.firstName}</h1><p>{user?.tenant.name}</p></div></header><div className="grid">{[['Usuarios','Gestión segura por tenant'],['Roles','Permisos granulares'],['Auditoría','Actividad reciente']].map(x=><article key={x[0]}><strong>{x[0]}</strong><small>{x[1]}</small></article>)}</div></>}
