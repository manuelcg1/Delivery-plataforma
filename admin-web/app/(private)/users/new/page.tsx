'use client';import{FormEvent,useState}from'react';import{api}from'@/lib/api';import{useRouter}from'next/navigation';export default function NewUser(){const[d,setD]=useState({email:'',password:'',firstName:'',lastName:'',phone:''});const r=useRouter();async function submit(x:FormEvent){x.preventDefault();await api('/api/v1/users',{method:'POST',body:JSON.stringify({...d,roleIds:[]})});r.push('/users')}return <><h1>Nuevo usuario</h1><form className="panel form" onSubmit={submit}>{Object.entries(d).map(([k,v])=><label key={k}>{k}<input type={k==='password'?'password':k==='email'?'email':'text'} value={v} onChange={x=>setD({...d,[k]:x.target.value})}/></label>)}<button>Crear usuario</button></form></>}
