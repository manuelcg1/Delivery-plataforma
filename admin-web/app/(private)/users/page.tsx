'use client';import{useEffect,useState}from'react';import Link from'next/link';import{api}from'@/lib/api';type U={id:string;email:string;firstName:string;lastName:string;status:string;roles:string[]};export default function Users(){const[rows,setRows]=useState<U[]>([]);useEffect(()=>{api<U[]>('/api/v1/users').then(setRows)},[]);return <><div className="title"><h1>Usuarios</h1><Link className="button" href="/users/new">Nuevo usuario</Link></div><section className="panel table">{rows.map(u=><Link className="tr" href={`/users/${u.id}`} key={u.id}><span>{u.firstName} {u.lastName}</span><span>{u.email}</span><span>{u.roles.join(', ')}</span><span>{u.status}</span></Link>)}</section></>}
