'use client';import{useEffect,useState}from'react';import{api}from'@/lib/api';import'../platform.css';type Row={id:string;tenant:string;merchant:string;orderNumber:string;type:string;amount:number;currency:string;status:string;createdAt:string};export default function Page(){const[rows,setRows]=useState<Row[]>([]);useEffect(()=>{api<Row[]>('/api/v1/platform/transactions').then(setRows)},[]);return <><header className="platform-head"><div><h1>Transacciones</h1><p>Pagos, capturas y reembolsos de toda la plataforma.</p></div></header><div className="panel platform-wrap"><table className="platform-table"><thead><tr><th>Fecha</th><th>Pedido</th><th>Tenant</th><th>Comercio</th><th>Tipo</th><th>Estado</th><th>Monto</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.createdAt).toLocaleString('es-PE')}</td><td>{x.orderNumber}</td><td>{x.tenant}</td><td>{x.merchant}</td><td>{x.type}</td><td>{x.status}</td><td>{x.currency} {Number(x.amount).toFixed(2)}</td></tr>)}</tbody></table></div></>}
