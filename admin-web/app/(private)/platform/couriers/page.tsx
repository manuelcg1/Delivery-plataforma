'use client';import{useEffect,useState}from'react';import{api}from'@/lib/api';import'../platform.css';type Row={id:string;tenant:string;name:string;status:string;vehicle:string;zone:string|null;activeOrders:number;completedOrders:number;rating:number;lastConnection:string|null};export default function Page(){const[rows,setRows]=useState<Row[]>([]);useEffect(()=>{api<Row[]>('/api/v1/platform/couriers').then(setRows)},[]);return <><header className="platform-head"><div><h1>Repartidores</h1><p>Disponibilidad, operación y rendimiento global.</p></div></header><div className="panel platform-wrap"><table className="platform-table"><thead><tr><th>Repartidor</th><th>Tenant</th><th>Estado</th><th>Vehículo</th><th>Zona</th><th>Activos</th><th>Completados</th><th>Calificación</th><th>Última conexión</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.tenant}</td><td>{x.status}</td><td>{x.vehicle}</td><td>{x.zone||'—'}</td><td>{x.activeOrders}</td><td>{x.completedOrders}</td><td>{Number(x.rating).toFixed(1)}</td><td>{x.lastConnection?new Date(x.lastConnection).toLocaleString('es-PE'):'—'}</td></tr>)}</tbody></table></div></>}
