'use client';import Link from'next/link';import{use,useEffect,useState}from'react';import{api}from'@/lib/api';type M={id:string;code:string;name:string;description:string;merchantType:string;status:string;defaultCurrency:string;timezone:string};export default function Merchant({params}:{params:Promise<{id:string}>}){const{id}=use(params);const[m,setM]=useState<M|null>(null);useEffect(()=>{api<M>(`/api/v1/merchants/${id}`).then(setM)},[id]);if(!m)return <div className="empty">Cargando comercio…</div>;return <><div className="title"><div><p className="eyebrow">{m.merchantType}</p><h1>{m.name}</h1><p>{m.description||'Sin descripción'}</p></div><span className={`badge ${m.status.toLowerCase()}`}>{m.status}</span></div><div className="catalog-cards"><Link className="panel action-card" href={`/merchants/${id}/branches`}><strong>Sucursales</strong><span>Direcciones y disponibilidad</span></Link><Link className="panel action-card" href={`/merchants/${id}/categories`}><strong>Categorías</strong><span>Organiza el catálogo</span></Link><Link className="panel action-card" href={`/merchants/${id}/products`}><strong>Productos</strong><span>Precios, publicación e inventario</span></Link></div><section className="panel"><h2>Configuración</h2><p>Código: {m.code}</p><p>Moneda: {m.defaultCurrency}</p><p>Zona horaria: {m.timezone}</p></section></>}
