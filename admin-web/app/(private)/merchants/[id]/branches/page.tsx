'use client';import{use,useEffect,useState}from'react';import{api}from'@/lib/api';type B={id:string;name:string;code:string;addressLine:string;district:string;status:string};export default function Branches({params}:{params:Promise<{id:string}>}){const{id}=use(params);const[rows,setRows]=useState<B[]>([]);useEffect(()=>{api<B[]>(`/api/v1/merchants/${id}/branches`).then(setRows)},[id]);return <><p className="eyebrow">CATÁLOGO</p><h1>Sucursales</h1><section className="panel table">{rows.length===0?<div className="empty"><strong>Sin sucursales</strong><span>Créala desde Swagger mientras se completa el formulario avanzado.</span></div>:rows.map(x=><div className="tr" key={x.id}><span><strong>{x.name}</strong><small>{x.code}</small></span><span>{x.addressLine}</span><span>{x.district}</span><span className={`badge ${x.status.toLowerCase()}`}>{x.status}</span></div>)}</section></>}
