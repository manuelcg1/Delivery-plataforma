'use client';import{useEffect,useState}from'react';import Link from'next/link';import{api}from'@/lib/api';type R={id:string;name:string;code:string;permissions:string[]};export default function Roles(){const[rows,setRows]=useState<R[]>([]);useEffect(()=>{api<R[]>('/api/v1/roles').then(setRows)},[]);return <><div className="title"><h1>Roles</h1><Link className="button" href="/roles/new">Nuevo rol</Link></div><section className="panel table">{rows.map(r=><Link className="tr" href={`/roles/${r.id}`} key={r.id}><span>{r.name}</span><span>{r.code}</span><span>{r.permissions.length} permisos</span></Link>)}</section></>}
