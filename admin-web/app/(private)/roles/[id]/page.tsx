'use client';import{useEffect,useState,use}from'react';import{api}from'@/lib/api';type R={name:string;code:string;description:string;permissions:string[]};export default function Role({params}:{params:Promise<{id:string}>}){const{id}=use(params);const[r,setR]=useState<R|null>(null);useEffect(()=>{api<R>(`/api/v1/roles/${id}`).then(setR)},[id]);return <><h1>{r?.name??'Rol'}</h1><section className="panel"><p>{r?.code}</p><p>{r?.description}</p><div className="chips">{r?.permissions.map(p=><span key={p}>{p}</span>)}</div></section></>}
