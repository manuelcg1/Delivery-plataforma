'use client';import{FormEvent,useState}from'react';import{api}from'@/lib/api';import{useRouter}from'next/navigation';export default function NewRole(){const[d,setD]=useState({code:'',name:'',description:'',active:true});const r=useRouter();async function submit(e:FormEvent){e.preventDefault();await api('/api/v1/roles',{method:'POST',body:JSON.stringify(d)});r.push('/roles')}return <><h1>Nuevo rol</h1><form className="panel form" onSubmit={submit}><label>Código<input value={d.code} onChange={x=>setD({...d,code:x.target.value.toUpperCase()})}/></label><label>Nombre<input value={d.name} onChange={x=>setD({...d,name:x.target.value})}/></label><label>Descripción<input value={d.description} onChange={x=>setD({...d,description:x.target.value})}/></label><button>Crear rol</button></form></>}
