'use client';import{useEffect,useState}from'react';import Link from'next/link';import{api}from'@/lib/api';import type{Order}from'@/lib/orders';import{OrderCard}from'@/components/orders/OrderComponents';
export default function Orders(){const[orders,setOrders]=useState<Order[]|null>(null);useEffect(()=>{api<Order[]>('/api/v1/orders').then(setOrders)},[]);return <><div className="title"><div><p className="eyebrow">PEDIDOS</p><h1>Mis pedidos</h1><p>Consulta el estado y detalle de tus compras.</p></div><Link className="button" href="/cart">Ver carrito</Link></div>{orders===null?<div className="ui-loader">Cargando pedidos…</div>:orders.length===0?<div className="panel orders-empty"><strong>Aún no tienes pedidos</strong><span>Tu historial aparecerá aquí después del checkout.</span><Link className="button" href="/cart">Ir al carrito</Link></div>:<section className="orders-list">{orders.map(x=><OrderCard key={x.id} order={x}/>)}</section>}</>}
