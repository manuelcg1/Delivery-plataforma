'use client';import{FormEvent,useState}from'react';import{api}from'@/lib/api';export default function Forgot(){const[tenantCode,setT]=useState('');const[email,setE]=useState('');const[done,setDone]=useState(false);async function submit(e:FormEvent){e.preventDefault();await api('/api/v1/auth/forgot-password',{method:'POST',body:JSON.stringify({tenantCode,email})});setDone(true)}return <main className="auth"><form onSubmit={submit}><h1>Recuperar contraseña</h1>{done?<p>Si la cuenta existe, recibirás un correo con instrucciones.</p>:<><label>Empresa<input value={tenantCode} onChange={x=>setT(x.target.value)}/></label><label>Correo<input type="email" value={email} onChange={x=>setE(x.target.value)}/></label><button>Enviar enlace</button></>}</form></main>}
